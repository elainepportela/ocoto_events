defmodule OctoEventsWeb.PageControllerTest do
  use OctoEventsWeb.ConnCase

  # test "POST /", %{conn: conn} do
  #   conn = get(conn, "/")
  #   assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  # end

end
